### Review Comment on Pull Request #59

I suggest replacing hardcoded hex values like `bg-[#050505]` in `page.tsx` with semantic tokens defined in `tailwind.config.ts`. For example, define `colors.background.primary` in the config and use `bg-background-primary` instead. This ensures consistency and makes theme updates easier.